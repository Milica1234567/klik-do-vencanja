/**
 * Interactive opener scene for Envelope Romance.
 * Implement when Canva layers are available:
 * body, flap, seal, paper (+ optional florals).
 *
 * Phases from config: closed → flap-opening → card-rising → enter
 */

function EnvelopeOpenScene() {
  return null;
}

export default EnvelopeOpenScene;
